Optional extra to make DynamicFPS change the shadow resolution for you.
Make sure to also enable the main DynamicFPS mod marked (Required) for this to be active.
As with the game resolution options this requires "alternate / extended memory layout" setting enabled in emulator